/**
 * Copy to intake-config.js (do not commit the real secret).
 * Must match STRATEGY_INTAKE_WEBHOOK_SECRET on Vercel operator production.
 */
window.MODA_STRATEGY_INTAKE_WEBHOOK_SECRET = 'replace-with-production-webhook-secret';
